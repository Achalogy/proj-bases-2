CREATE TABLE Puntos (
  valor INT NOT NULL 
    CHECK (
      valor > 0
    )
);

CREATE TABLE Miembro (
    ID_Miembro NUMBER GENERATED BY DEFAULT ON NULL AS IDENTITY,
    Tipo_M
bem

    PRIMARY KEY (ID_Miembro)
  
);

CREATE TABLE Edificio (
  ID_Edificio NUMBER GENERATED BY DEFAULT ON NULL AS IDENTITY,
  
  PRIMARY KEY (ID_Edificio)
);

CREATE TABLE Piso (
  ID_Piso NUMBER GENERATED BY DEFAULT ON NULL AS IDENTITY,
  ID_Edificio NUMBER NOT NULL,

  PRIMARY KEY (ID_Piso),
  FOREIGN KEY (ID_Edificio) REFERENCES Edificio(ID_Edificio)
    ON DELETE SET NULL
);

CREATE TABLE Cafeteria (
  ID_Cafeteria NUMBER GENERATED BY DEFAULT ON NULL AS IDENTITY,
  Nombre VARCHAR2(255) NOT NULL,
  ID_Piso NUMBER NOT NULL,
  ID_Edificio NUMBER NOT NULL,

  PRIMARY KEY (ID_Cafeteria),
  FOREIGN KEY (ID_Piso) REFERENCES Piso(ID_Piso)
    ON DELETE SET NULL,
  FOREIGN KEY (ID_Edificio) REFERENCES Edificio(ID_Edificio)
    ON DELETE SET NULL
);
CREATE TABLE Producto ();
CREATE TABLE Colaborador ();
CREATE TABLE InventarioCafeteria ();
CREATE TABLE CompraxProducto ();
CREATE TABLE Compra (); 
CREATE TABLE ImpuestoXCompra ();
CREATE TABLE TxPuntos ();
CREATE TABLE Pago ();




